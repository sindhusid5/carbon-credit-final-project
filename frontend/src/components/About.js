import React from "react";
import AboutCarousel from "./AboutCarousel"; // Path to AboutCarousel component

const About = () => {
  return (
    <section className="about">
      <h2>Project Vision</h2>
      <AboutCarousel />
    </section>
  );
};

export default About;
